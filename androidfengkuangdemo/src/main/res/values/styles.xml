<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="dialog" parent="@style/Theme.AppCompat.Dialog">

        <!-- 去黑边 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置是否可滑动 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 设置是否透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景 -->
        <item name="android:background">@null</item>
        <!-- 窗口背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否变暗 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 点击空白部分activity不消失 -->
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
    <!--<color name="colorPrimaryNight">#3b3b3b</color>-->
    <!--<color name="colorPrimaryDarkNight">#383838</color>-->
    <!--<color name="colorAccentNight">#a72b55</color>-->
    <!--<color name="textColorNight">#FFFFFF</color>-->
    <!--<color name="backgroundColorNight">#3b3b3b</color>-->

    <style name="NightAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="textColor">@color/colorAccentNight</item>
        <item name="buttonTextColor">@color/textColorNight</item>
        <item name="mainBackground">@color/backgroundColor</item>
    </style>
    <style name="DayAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="textColor">@color/colorAccent</item>
        <item name="buttonTextColor">@color/textColor</item>
        <item name="mainBackground">@color/backgroundColorNight</item>
    </style>


</resources>
